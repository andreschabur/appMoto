{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Administrators</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ entity.userName }}</td>
            </tr>
            <tr>
                <th>Userpassword</th>
                <td>{{ entity.userPassword }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('backend_administrador') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('backend_administrador_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
