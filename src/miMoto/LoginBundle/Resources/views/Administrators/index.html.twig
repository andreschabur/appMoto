{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Administrators list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Username</th>
                <th>Userpassword</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('backend_administrador_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.userName }}</td>
                <td>{{ entity.userPassword }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('backend_administrador_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('backend_administrador_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('backend_administrador_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
